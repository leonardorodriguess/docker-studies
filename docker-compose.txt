//instalar docker-compose
sudo curl -SL https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-linux-x86_64 -o>
sudo chmod +x /usr/local/bin/docker-compose


//parar rodar o docker-compose
sudo docker-compose up
